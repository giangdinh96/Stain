package ${packageName};

import ${pkName}.base.BasePresenter;

public class ${className}Presenter extends BasePresenter<I${className}View> implements I${className}Presenter {
  @Override
  public void start() {

  }
}
